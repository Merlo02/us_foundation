# Experiment C — WebDataset IO
# --------------------------------------------------------------------
# Switch the DataModule to WebDataset tar shards. No epoch-based
# subsampling (B3 is HDF5-only by design).
# --------------------------------------------------------------------

defaults: [base]

data:
  format: webdataset
  sampling_strategy: naive

train:
  run_name: exp_C_webdataset
